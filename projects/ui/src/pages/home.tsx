import MapComponent from '../components/map';
import PageLayout from '../components/page-layout';

const Home = () => {
  return (
    <PageLayout>
      <MapComponent />
    </PageLayout>
  );
};

export default Home;
